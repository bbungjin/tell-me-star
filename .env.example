# Pages 또는 `npm run pages:dev` 와 같은 출처이면 비움 → `/api/chart-interpret`
# CRA만 localhost:3000으로 띄우고 API는 다른 호스트일 때만 베이스 URL 지정 (슬래시 없음)
# REACT_APP_API_BASE_URL=https://xxxx.pages.dev
REACT_APP_API_BASE_URL=
