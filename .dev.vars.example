# 로컬에서 `npm run pages:dev` 전에:
# 1) 이 내용을 참고해 프로젝트 루트에 `.dev.vars` 파일을 새로 만든다 (파일명 앞의 점 포함).
# 2) 아래 한 줄을 넣되, 값은 OpenAI 대시보드에서 발급한 키로 바꾼다. `.dev.vars`는 Git에 커밋하지 않는다.
#
# OPENAI_API_KEY=(여기에 키 붙여넣기)
#
# 선택 — Functions에서 특정 오리진만 허용할 때:
# ALLOWED_ORIGINS=http://localhost:8788,http://127.0.0.1:8788
